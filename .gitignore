# executables
/mosaic
/histogram
/playground

# compiled stuff in compiled directory
compiled/*

# exception for gitkeep so we're able to add the directory
!compiled/.gitkeep

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
